# Core/configs/commands_config.py

DEFAULT_COMMANDS_CONFIG = {
    "keywords_enabled": True,
    "keywords": {
        "CMD":    "/",   # prefix: //   (generic command search)
        "FX.V":   "V",   # prefix: /V   (video effects)
        "FX.A":   "A",   # prefix: /A   (audio effects)
        "TR.V":   "T",   # prefix: /T   (video transitions)
        "PRST":   "P",   # prefix: /P   (presets)
        "CMD.QA": "QA",  # prefix: /QA  (Quick Apply mode)
        "CMD.BM": "BM",  # prefix: /BM  (Better Motion mode)
    }
}
